/* <input
                type="range"
                max={song?.duration ? song?.duration : `${song?.duration}`}
                className="progress"
               style={{ background: trackStyling }}
          />		*/